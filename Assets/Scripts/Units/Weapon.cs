namespace RpgGame.Units
{
    public enum Weapon { MainWeapon, AdditionalWeapon }

    public enum WeaponSet : byte
    {
        None = 0,
        SwordAndShield,
        Bow,
        TwoHandedSword,
        Magic
    }
}